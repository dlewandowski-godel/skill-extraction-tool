# PostgreSQL
POSTGRES_DB=skillextractor
POSTGRES_USER=skilluser
POSTGRES_PASSWORD=changeme

# pgAdmin
PGADMIN_EMAIL=admin@example.com
PGADMIN_PASSWORD=changeme

# JWT
JWT_SECRET=your-super-secret-jwt-key-change-in-production-min-32-chars

# Seed admin account
SEED_ADMIN_EMAIL=admin@skillextractor.local
SEED_ADMIN_PASSWORD=Admin@123!

# Seed demo employees (auto-created on first run)
SEED_EMPLOYEE_PASSWORD=Employee@123!

# Set to true to wipe demo data and re-seed from scratch on next startup
# WARNING: deletes all non-admin employees, their documents and skills
SEED_FORCE_RESEED=false

# ASP.NET Core
ASPNETCORE_ENVIRONMENT=Development

# Frontend
VITE_API_URL=http://localhost:5000
